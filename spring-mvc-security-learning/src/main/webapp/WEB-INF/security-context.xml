<beans:beans xmlns:sec="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security-4.0.xsd">

    <!--&lt;!&ndash; http block: enables web security, has the effect of creating the chain of filters that handle spring security stuff&ndash;&gt;-->
    <!--&lt;!&ndash; use expressions enables spring expression language to be used in JSPs &ndash;&gt;-->
    <!--<sec:http auto-config="true" use-expressions="true">-->

        <!--<sec:intercept-url pattern="/test/securitylearning/user**" access="user"/>-->

        <!--&lt;!&ndash; note the use of ** in the URLs, to stop hacker getting past by appending stuff to it.  &ndash;&gt;-->

        <!--<sec:intercept-url pattern="/test/securitylearning/admin**" access="admin"/>-->

        <!--&lt;!&ndash; Tells it to use 'remember-me' authentication. This means that it will store a cookie on the browser to keep a user logged-->
        <!--in between sessions. Presumably you can configure things like cookie expiry.&ndash;&gt;-->
        <!--<sec:remember-me />-->
    <!--</sec:http>-->

    <!--&lt;!&ndash; here we define the classes used for authentication. Basically we have: AuthenticationManager has a AuthenticationProvider-->
    <!--has a UserDetailsService. It is the user details service which provides a look up of users based on username, &ndash;&gt;-->
    <!--<sec:authentication-manager>-->
        <!--<sec:authentication-provider>-->
            <!--&lt;!&ndash;this gives us a default noddy UserDetailsService. In practice you'd use something proper that e.g. went off to a database&ndash;&gt;-->
            <!--<sec:user-service>-->
                <!--<sec:user name="hedges_user" password="password" authorities="user" />-->
                <!--<sec:user name="hedges_admin" password="password" authorities="admin" />-->
            <!--</sec:user-service>-->
        <!--</sec:authentication-provider>-->
    <!--</sec:authentication-manager>-->
</beans:beans>